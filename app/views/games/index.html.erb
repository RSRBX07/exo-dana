<p id="notice"><%= notice %></p>

<h1>Les tirage de loto</h1>

    <% @games.each do |game| %>
      <div>
        <%= link_to game.name, 'games_show_path' %>
      </div>
    <% end %>

     <%= link_to 'description', 'games_show_path' %>



